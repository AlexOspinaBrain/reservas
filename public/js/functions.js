document.addEventListener("DOMContentLoaded", function() {

    const dataURL = document.location.toString().split('/');
    
    if (dataURL[dataURL.length - 1] === 'reservas') {
        const teatro = document.getElementsByName('teatro');
        const filas = 5;
        const columnas = 10;
        for (let i = 0; i < filas; i++){
            
        }
    }

});